import React from "react";
import { StyleSheet, Text, View } from "react-native";

const createIngredient = () => {
  return (
    <View>
      <Text>createIngredient</Text>
    </View>
  );
};

export default createIngredient;

const styles = StyleSheet.create({});
